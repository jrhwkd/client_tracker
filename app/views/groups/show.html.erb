<p id="notice"><%= notice %></p>

<p>
  <strong>Company:</strong>
  <%= Company.find(@group.company_id).name %>
</p>

<h1>
  <%= @group.name %> Group
</h1>

<p>
  <strong>Managers:</strong>
  <ul>
    <% @group.managers.each do |manager| %>
    <li><%= manager.name %> | <%= link_to 'Show', manager_path %></li>
    <% end %>
  </ul>
</p>  

<%= link_to 'New Manager', new_manager_path(@group) %> |
<%= link_to 'Edit Group', edit_group_path(@group) %> |
<%= link_to 'Back', "/companies/#{@group.company_id}" %>
